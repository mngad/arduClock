# arduClock
Clock for 4, 8x8 LED matrices connected to an arduino

#LedControlMS_mine
Rotates the text by 90 degrees and takes an extra argument in `LedControl::displayChar(int matrix, int charIndex, int pos)` allowing the positioning of a charactor by `+/- n` leds
